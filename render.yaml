services:
  - type: web
    name: skillforge-backend
    env: node
    region: singapore
    buildCommand: npm install && npm run setup:python
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production